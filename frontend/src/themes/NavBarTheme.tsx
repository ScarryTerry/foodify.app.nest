import { createTheme } from '@mui/material/styles';

const NavBarTheme = createTheme({
  palette: {
    primary: {
      main: '#33691e',
    },
    secondary:{
      main: '#FFFF'
    } 
  },
});

export default NavBarTheme;
